name: Release
on:
  push:
    branches:
      - "main"
  workflow_dispatch:
    inputs:
      name:
        type: choice
        options:
          - monalisa
          - cschleiden
        description: Who to greet
      use-emoji:
        type: boolean
        description: Include  emojis
jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
