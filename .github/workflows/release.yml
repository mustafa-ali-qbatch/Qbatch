name: Input-workflow
on:
  workflow_dispatch:
    inputs:
      name:
        description: "Enter Commit Message"
        default: "message"
        required: true
jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: Show Commit Message
        run: echo "Commit Message is  ${{ github.event.inputs.name }}"
