run-name: Deploy to ${{ github.event.inputs.name }} by @${{ github.actor }}
on:
  workflow_dispatch:
    inputs:
      name:
        description: "Enter Commit Message"
        default: "message"
        required: true
jobs:
  message:
    runs-on: ubuntu-latest
    steps:
      - name: Show Commit Message
        run: echo "Commit Message':'  ${{ github.event.inputs.name }}"
